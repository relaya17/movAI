# Local TLS workaround: corporate/antivirus HTTPS interception breaks
# registry.npmjs.org certificate verification (UNABLE_TO_VERIFY_LEAF_SIGNATURE).
# Prefer fixing the system trust store; remove this once installs work without it.
strict-ssl=false
